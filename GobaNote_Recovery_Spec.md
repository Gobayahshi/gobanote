# Goba.note(고바노트) 복구·재구축 기능 명세서

## 1. 문서 목적

이 문서는 사라진 원본 Expo 소스코드를 그대로 복원하는 문서가 아니다. 남아 있는 Cursor 대화 기록을 근거로 **고바노트의 기능과 사용 경험을 재구축**하기 위한 기준 문서다.

### 최우선 원칙

> 빈 칸에 적으면 AI가 글의 종류를 판단하고, 그 글에 필요한 기능만 조용히 열어 준다.

- 고바노트는 기도 앱이 아니라, 일상·할 일·기도·일기·감사·말씀을 한 곳에 적는 한국어 노트 앱이다.
- 사용자는 쓰기 전에 폴더·분류·템플릿을 고르지 않아도 된다.
- AI 분류가 틀릴 수 있으므로, 사용자가 항상 분류를 수정할 수 있어야 한다.
- 새 기능을 많이 넣기보다, 적기 → 자동 정리 → 다시 찾기 → 필요한 일만 챙기기 흐름을 우선한다.

---

## 2. 복구 작업의 범위와 안전 규칙

### 프로젝트 위치

- 로컬 프로젝트 폴더: `C:\Users\SKTelecom\Documents\(26년08월) Goba.note`
- GitHub 저장소 이름 권장안: `gobanote`
- 저장소 공개 범위: **Private**

### 시작 전 필수 작업

1. 위 폴더가 비어 있는지, 또는 예전 파일이 남아 있는지 먼저 읽기 전용으로 확인한다.
2. 기존 파일이 있으면 덮어쓰거나 삭제하지 말고, 목록과 상태를 먼저 보고한다.
3. Git 저장소를 초기화하고 `.gitignore`와 `.env.example`을 먼저 만든다.
4. GitHub 원격 저장소를 연결한 뒤, 첫 커밋과 첫 push가 성공한 것을 확인한다.
5. 이후에는 기능 단위가 끝날 때마다 커밋하고 원격 저장소에 push한다.

### 절대 지켜야 할 규칙

- API 키, Supabase anon key, Gemini 키, access token을 채팅·커밋·소스코드에 직접 쓰지 않는다.
- 실제 Supabase의 기존 `notes` 데이터는 삭제·초기화·마이그레이션하지 않는다. 먼저 스키마와 데이터를 읽기 전용으로 확인하고 보고한다.
- 예전 Expo Go 터널 주소, 임시 Vercel 주소, Cloudflare 임시 주소를 복구 대상으로 삼지 않는다.
- 초기 버전에서 전체 노트 AI 채팅, 전체 그래프, 자동 목록 추출, 외부 앱 공유 시트는 구현하지 않는다.
- Expo Go 테스트를 위한 Metro 실행은 구현 후 검증 단계에서만 한다. 반복적인 배포 요청은 기능 작업으로 간주하지 않는다.

---

## 3. 권장 기술 구조

| 구분 | 선택 |
|---|---|
| 모바일 앱 | Expo + React Native + TypeScript |
| 데이터·로그인 | Supabase Auth + Postgres |
| AI 분류 | Supabase Edge Function `classify-note` → Gemini API |
| 로컬 설정 | `.env` 파일, Git 추적 제외 |
| 테스트 | Expo Go 실기기 테스트 + Expo web/단위 테스트(가능한 범위) |

### AI 분류 구조

1. 사용자가 노트를 저장한다.
2. 앱은 Supabase에 노트를 저장한다.
3. 앱은 로그인 사용자 JWT와 `note_id`를 포함해 `classify-note`를 호출한다.
4. Edge Function이 Gemini에 제목·본문을 보내 분류 결과를 받는다.
5. Edge Function은 분류 결과를 노트에 저장하고, 앱은 최신 결과를 다시 표시한다.

Gemini API 키는 반드시 Edge Function의 서버 비밀값으로만 사용한다. Expo 앱에 Gemini 키를 넣지 않는다.

---

## 4. 데이터 모델

기존 Supabase 스키마가 남아 있으면 이를 우선 확인하고, 아래 구조와 비교해 부족한 부분만 **추가 마이그레이션**으로 보완한다.

### `notes` 핵심 필드

| 필드 | 의미 |
|---|---|
| `id` | 노트 ID |
| `user_id` | 작성자 ID |
| `title` | 제목 |
| `content` | 본문 |
| `category` | 아래 여섯 분류 중 하나 |
| `category_confidence` | AI 분류 확신도(0~100) |
| `category_reason` | 한국어 분류 이유 |
| `topic` | 해당 메모 내용에서만 뽑은 짧은 주제 |
| `tags` | 선택적 태그 목록 |
| `deadline_at` | 할 일·기도 등의 기한 |
| `is_done` | 할 일 완료 여부 |
| `prayer_answer_status` | `yes`, `no`, `wait`, 또는 비어 있음 |
| `created_at`, `updated_at` | 생성·수정 시각 |

### 분류 값

| 코드 | 화면 이름 | 핵심 의미 |
|---|---|---|
| `daily` | 일상 | 일반 메모, 링크, 짧은 기록 |
| `todo` | 할 일 | 해야 할 행동·기한·체크리스트 |
| `prayer` | 기도 | 기도 제목·사람별 기도·기도 요청 |
| `journal` | 일기 | 하루의 감정·경험·성찰 |
| `gratitude` | 감사 | 감사 기록과 감사 달력 |
| `scripture` | 말씀 | 성경 구절·묵상용 말씀 본문 |

### 선택적 사용량 기록

- `classify_usage`는 분류 호출 횟수 추적용이다.
- 기존 테이블이 없다면 노트의 핵심 기능보다 뒤 단계에서 만든다.
- 없어도 분류 자체가 실패하면 안 된다.

---

## 5. 화면과 사용자 흐름

### 하단 탭

| 탭 | 역할 |
|---|---|
| 쓰기 | 새 노트 작성과 자동·수동 분류 |
| 노트 | 전체 노트 목록, 필터, 노트 상세 |
| 검색 | 제목·본문·주제·태그 기반 검색 |
| 감사 | 감사 달력과 감사 노트 모아보기 |
| 챙김 | 아직 완료하지 않은 할 일을 빠르게 확인·완료 처리 |
| 계정 | 로그인·로그아웃·알림·개인 설정 |

모든 탭을 한 화면에 길게 쌓지 않는다. 휴대폰에서 각 역할이 분명하도록 탭 기반 화면을 유지한다.

### 쓰기 화면

1. 제목과 본문을 자유롭게 입력한다.
2. 저장 전 또는 저장 후 AI가 분류 결과를 보여 준다.
3. 사용자는 분류를 수동으로 바꿀 수 있다.
4. 결과에는 화면 이름, 짧은 한국어 이유, 확신도가 보인다.
5. 노트에 필요한 기능만 추가로 연다. 예: 할 일은 기한·완료, 기도는 응답 상태.

### 노트 목록·상세

- 필터: 전체 / 일상 / 할 일 / 기도 / 일기 / 감사 / 말씀
- 오래된 노트를 찾기 쉬운 날짜순 목록
- 노트 상세에는 본문과 그 분류에 필요한 조작만 표시한다.
- 삭제 전 확인 창을 두고, 가능하면 최근 삭제 되돌리기를 지원한다.

---

## 6. 분류별 기능 명세

### 6.1 일상 (`daily`)

- 기본 메모 기능만 제공한다.
- 메모 내용에서만 주제를 정한다. 과거 메모의 주제를 현재 메모에 억지로 붙이지 않는다.
- URL·YouTube 링크는 링크 또는 카드로 표시할 수 있다.
- 기한, 알림, 성찰 질문은 기본으로 열지 않는다.

### 6.2 할 일 (`todo`)

- 완료 체크: `is_done`
- 기한 입력: `deadline_at`
- 본문에 날짜·시간이 명확하면 기한 후보를 제안할 수 있다.
- 알림은 사용자가 허용한 경우에만 예약한다.
- `챙김` 탭에는 미완료 할 일을 우선 보여 주고, 그 화면에서 바로 완료 처리할 수 있어야 한다.

### 6.3 기도 (`prayer`)

- 화면의 주제/칩 명칭은 **기도 제목**으로 표시한다. `중보`라는 표현은 사용하지 않는다.
- 선택적으로 기한과 알림을 설정할 수 있다.
- 응답 상태 선택: **Yes / No / Wait**
- 기도 내용에 육아·직장 같은 단어가 있어도, 현재 내용이 기도라면 업무 아이디어 같은 과거 주제를 붙이지 않는다.

### 6.4 일기 (`journal`)

- 하루의 감정·경험·성찰 기록
- 짧은 성찰 질문을 제안하거나 표시한다.
- 기한·알림을 기본으로 강요하지 않는다.

### 6.5 감사 (`gratitude`)

- 수동 분류에서 감사를 선택할 수 있다.
- 선택 시 제목을 `YYMMDD 감사` 형식으로 자동 채운다. 사용자는 수정할 수 있다.
- 본문에는 아래 네 질문을 넣고, 각 질문 아래에 2~3줄 정도 답할 공간을 둔다.
  - 베풀어 주신 은혜 감사
  - 들어주신 기도 감사
  - 거절하신 것 감사
  - 미리 감사
- 저장한 날짜는 감사 달력에 표시한다.
- Gemini 자동 분류도 감사 표현을 `gratitude`로 분류할 수 있어야 한다.

### 6.6 말씀 (`scripture`)

- 성경 구절·성경 본문은 기도가 아니라 말씀으로 분류한다.
- 성경책+장절 표기, 약식 장절 표기, 성경 문체를 함께 고려한다.
- “해주세요”, “기도 제목”, 사람별 기도 요청처럼 현재 요청하는 문장은 기도로 분류한다.
- 성경 본문으로 판단되면 저장 전에 짧게 알려 준다.
- 이미 잘못 분류된 노트는 사용자가 다시 분류할 수 있어야 한다.

---

## 7. AI 분류 규칙

### 응답 형식

AI는 적어도 다음을 반환한다.

```json
{
  "category": "todo",
  "confidence": 95,
  "reason": "내일까지 해야 할 행동과 기한이 포함되어 있어 할 일로 분류했습니다.",
  "topic": "장보기"
}
```

### 품질 규칙

- 제목을 사용자가 입력했으면 AI가 바꾸지 않는다.
- 제목이 비었을 때만 짧은 제목을 제안할 수 있다.
- 주제와 태그는 현재 노트 본문에 근거한 말만 사용한다.
- 분류 결과가 늦거나 실패해도 노트 저장 자체는 성공해야 한다.
- 자동 분류는 추천이며, 수동 분류가 항상 우선한다.
- 초기 버전에서는 개인 메모 전체를 AI에 미리 업로드하지 않는다. 분류할 노트만 Edge Function을 통해 보낸다.

---

## 8. 디자인 기준

- 전체 분위기: **진한 크림색 바탕 + 숲색/진한 그린 강조색**
- 일반 생산성 앱보다 부드럽고 조용한 느낌
- 한 화면에 기능을 과도하게 늘어놓지 않는다.
- 할 일·기도·감사처럼 특별한 노트에서만 관련 조작을 보여 준다.
- 모바일에서 한 손으로 쓰고 확인하기 좋은 큰 터치 영역과 명확한 글자 크기를 사용한다.

---

## 9. 이번 복구 범위에서 제외할 기능

아래는 대화에서 논의되었거나 제안되었지만, 초기 복구본에는 넣지 않는다.

- 전체 노트 내용을 기반으로 하는 AI 채팅/RAG
- 노트 간 전체 그래프 또는 복잡한 링크 맵
- 자동 목록 추출
- 다른 앱의 공유 시트에서 바로 노트를 만드는 기능
- App Store·TestFlight 정식 배포
- 임시 Vercel 공개 배포

이 기능들은 핵심 노트 경험이 안정된 뒤 별도 작업으로 진행한다.

---

## 10. 구현 순서와 완료 기준

### 단계 0 — 복구 준비

- 로컬 폴더 상태 확인
- Git 초기화, `.gitignore`, `.env.example`, 첫 커밋·push
- 기존 Supabase 프로젝트·테이블·함수의 읽기 전용 점검 보고

**완료 기준:** GitHub 원격 저장소에 첫 커밋이 존재하며, 비밀값은 커밋에 없음.

### 단계 1 — 앱 뼈대

- Expo TypeScript 앱
- 로그인 화면과 기본 하단 탭
- 노트 생성·수정·삭제·목록·상세

**완료 기준:** 로그인한 사용자가 노트를 작성·수정·삭제하고 목록에서 다시 열 수 있음.

### 단계 2 — 분류와 핵심 기능

- `classify-note` 연결
- 여섯 분류와 수동 분류
- 할 일 완료·기한·챙김
- 기도 응답 상태
- 감사 템플릿·감사 달력
- 말씀 분류 규칙

**완료 기준:** 각 분류별 대표 예시를 저장했을 때 적절한 화면 기능이 열리고, 사용자가 결과를 수정할 수 있음.

### 단계 3 — 검색·알림·정리

- 검색과 분류 필터
- 알림 권한 및 기한 알림
- 오류·빈 상태·분류 실패 처리
- 디자인 다듬기

**완료 기준:** Expo Go에서 핵심 흐름을 처음부터 끝까지 테스트하고, 알려진 오류를 목록으로 남김.

### 단계 4 — 사용자 확인 후 확장

- 오늘 할 일 상단 고정은 이 시점에 실제 사용 흐름을 보고 결정한다.
- 노트 AI 채팅, 공유 기능 등은 별도 기능 명세서를 만든 뒤 진행한다.

---

## 11. Claude Code 작업 방식

Claude Code는 다음 원칙으로 작업한다.

1. 전체 계획을 먼저 제시한다.
2. 한 번에 모든 기능을 만들지 않는다. 단계 하나를 끝내고 테스트·커밋·push한다.
3. 사용자에게 필요한 행동은 한 번에 한 가지로만 안내한다.
4. Supabase·GitHub 로그인·권한·비밀값 입력처럼 사용자 계정이 필요한 시점에는 멈추고 정확히 무엇을 해야 하는지 설명한다.
5. 파일을 만들거나 고친 뒤에는 변경 파일, 실행 방법, 테스트 결과, Git 커밋 해시를 짧게 보고한다.

### Claude Code에 처음 보낼 요청문

```text
첨부한 GobaNote_Recovery_Spec.md를 유일한 기능 기준으로 삼아 고바노트를 재구축해줘.

프로젝트는 C:\Users\SKTelecom\Documents\(26년08월) Goba.note 에 만들고, 시작 전에 해당 폴더의 기존 파일을 읽기 전용으로 확인해. 기존 파일이 있으면 절대 덮어쓰거나 삭제하지 말고 먼저 목록을 보고해.

그다음 Git을 초기화하고 .gitignore와 .env.example을 만든 뒤, GitHub private 저장소 gobanote를 연결해 첫 커밋과 push까지 완료해. 내 GitHub 로그인·저장소 생성·Supabase 로그인처럼 내 승인이 필요한 단계에서는 멈추고 한 번에 하나의 행동만 알려줘.

원본 코드를 찾는 작업이 아니라 명세서를 기반으로 한 재구축이다. 우선 단계 0의 점검 보고와 Git 백업만 끝내고, 앱 구현은 내 확인 후 시작해. Supabase의 기존 데이터는 절대 삭제·초기화·마이그레이션하지 말고 먼저 읽기 전용으로 상태를 확인해.
```

---

## 12. 복구 성공의 정의

다음이 모두 충족되면 1차 복구 성공이다.

- PC 지정 폴더에 Expo 프로젝트가 존재함
- GitHub private 저장소에 모든 소스와 명세서가 push됨
- Expo Go에서 로그인 → 메모 작성 → AI 분류 → 분류별 기능 사용 흐름이 동작함
- 감사 달력, 할 일 챙김, 기도 Yes/No/Wait, 말씀 분류가 확인됨
- 비밀값은 Git에 포함되지 않음
- 다음 작업부터는 코드가 사라지지 않도록 기능 단위 커밋이 유지됨
